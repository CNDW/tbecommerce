## Postgresql in docker for development
`docker run --name trashbags-pg -p 54322:5432 -v "$PWD":/tbecommerce -e POSTGRES_PASSWORD=password -e POSTGRES_USER=spree postgres:9.3`

### Connecting to docker
$ docker exec -it trashbags-pg bash

# su postgres

### restoring dumps

$ psql spree < <dumpfile>

current release sha 71d5eb8
