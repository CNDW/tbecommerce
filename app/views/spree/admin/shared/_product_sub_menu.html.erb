<% content_for :sub_menu do %>
  <ul id="sub_nav" data-hook="admin_product_sub_tabs" class="inline-menu">
    <%= tab :products, :match_path => '/products' %>
    <%= tab :color_types, {:match_path => '/color_types', label: 'color options'} %>
    <%= tab :color_values, {:match_path => '/color_values', label: 'colors'} %>
    <%= tab :option_types, {:match_path => '/option_types', label: 'custom options'} %>
    <%= tab :properties %>
    <%= tab :prototypes %>
    <%= tab :taxonomies %>
    <%= tab :taxons %>
  </ul>
<% end %>