div.modal
  div.modal-dialog
    div.modal-content
      div.modal-header
        button.close{action 'closeModal'} type="button"
          span aria-hidden="true" &times;
          span.sr-only Close
        h4 {{model.presentation}}
      div.modal-body
        img.img-responsive{bind-attr src=model.large_url}
        p {{model.description}}
div.modal-overlay
