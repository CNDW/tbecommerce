div.col-item-carousel.carousel.slide.well#product-carousel
	div.carousel-inner
		each image in images
			div.carousel-img.item.active
				img src=image.large_url alt="loading"
detail-thumbnail action="openModal" modalName="features" model=product
	img src="<%= asset_path('trashlogo.jpg') %>" alt="loading..."
	div.detail-caption
		h3 Product Features
		p.well
			|Each {{name}} is made with care to include the highest quality standard features.
	p Click for more!
detail-thumbnail
	img src="<%= asset_path('trashlogo.jpg') %>" alt="loading..."
	div.detail-caption
		h3 {{product.name}}
		p.well {{product.description}}
	p Click for more!
detail-thumbnail
	img src="<%= asset_path('trashlogo.jpg') %>" alt="loading..."
	div.detail-caption
		h3 {{product.name}}
		p.well {{product.description}}
	p Click for more!
detail-thumbnail
	img src="<%= asset_path('trashlogo.jpg') %>" alt="loading..."
	div.detail-caption
		h3 {{product.name}}
		p.well {{product.description}}
	p Click for more!