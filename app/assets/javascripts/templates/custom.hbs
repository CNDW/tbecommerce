<div class="custom-shop-navbar">
  <div class="col-sm-10 col-sm-offset-1">
    <ul class="custom-shop-navs">
      {{#link-to "custom.product" model tagName="li"}}
        <a>
          Item
        </a>
      {{/link-to}}
      {{#if hasProduct}}
        {{#link-to "custom.colors" model tagName="li"}}
          <a>
            Colors
          </a>
        {{/link-to}}
      {{/if}}
      {{#if hasProductAndColors}}
        {{#link-to "custom.features" model tagName="li"}}
          <a>
            Features
          </a>
        {{/link-to}}
      {{/if}}
    </ul>
  </div>
</div>
<div class="row">
  {{outlet}}
</div>